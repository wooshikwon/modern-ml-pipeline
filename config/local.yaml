# LOCAL 환경 설정
environment:
  app_env: local
  gcp_project_id: "test-project"

mlflow:
  tracking_uri: "./mlruns"
  experiment_name: "local-development"

serving:
  enabled: false
  host: "0.0.0.0"
  port: 8000

artifact_stores:
  local:
    enabled: true
    base_uri: "./artifacts"