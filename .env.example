# .env.example
# 이 파일은 프로젝트에 필요한 환경 변수를 정의하는 템플릿입니다.
# 실제 값으로 채운 뒤 `.env` 파일로 복사하여 사용하세요.
# `.env` 파일은 .gitignore에 의해 버전 관리에서 제외됩니다.

# 1. 실행 환경 설정
# 이 변수는 settings.py가 어떤 config/*.yaml 파일을 로드할지 결정합니다.
# APP_ENV=local: config/base.yaml + config/local.yaml (존재 시)
# APP_ENV=dev:   config/base.yaml + config/dev.yaml + config/local.yaml (존재 시)
# APP_ENV=prod:  config/base.yaml + config/prod.yaml
APP_ENV=local

# 2. GCP 관련 설정 (선택적)
# 로컬에서 GCP 리소스에 접근해야 할 경우에만 필요합니다.
# Docker 컨테이너나 GCP VM에서 실행 시에는 서비스 계정(SA)을 통해 자동 인증됩니다.
GCP_PROJECT_ID="your-gcp-project-id"
GCP_CREDENTIAL_PATH="./credentials/your-gcp-service-account.json"
GCS_BUCKET_NAME="your-ml-artifacts-bucket"

# 3. MLflow 및 기타 서비스 관련 설정 (선택적)
# config/*.yaml 파일의 기본값을 덮어쓰고 싶을 때 사용합니다.
MLFLOW_TRACKING_URI="http://127.0.0.1:5000"
REDIS_HOST="localhost"
REDIS_PORT=6379
MODEL_STAGE="Production"
